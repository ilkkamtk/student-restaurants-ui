const apiURL = import.meta.env.VITE_MAPBOX_TOKEN as string;
const mapBoxToken = import.meta.env.VITE_MAPBOX_TOKEN as string;
const uploadUrl = import.meta.env.VITE_UPLOAD_URL as string;
const googleApiKey = import.meta.env.VITE_GOOGLE_API_KEY as string;

export { apiURL, mapBoxToken, uploadUrl, googleApiKey };
